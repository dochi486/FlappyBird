using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Relocate : MonoBehaviour
{
    // Start is called before the first frame update
    //이거보다 위치 X가 작아지면 오른쪽으로 이미지크기 20.48 * 2만큼 오른쪽으로

    //[SerializeField] -> Private인 상태로 인스펙터에 노출만 되도록! 다른 객체(?)에서 참조는 못하게 하고 인스펙터에서 수정하고 싶을 때 사용. 
    //float TestminX = -13;

    // Update is called once per frame
    void Update()
    {
    
    }
}
